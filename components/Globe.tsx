"use client";
import { useEffect,useMemo,useRef,useState } from "react";
import { geoOrthographic,geoPath } from "d3-geo";
import { feature } from "topojson-client";
import countries from "world-atlas/countries-110m.json";
type Lead={name:string;lat:number|null;lng:number|null;rating:number|null;review_count:number;city:string;state:string;domain:string};
type Props={location:string;onLocation:(name:string)=>void;leads?:Lead[]};
type Country={type:"Feature";id?:string|number;properties?:Record<string,any>;geometry:any};
const names:Record<string,string>={"356":"India","840":"United States","124":"Canada","826":"United Kingdom","36":"Australia","276":"Germany","392":"Japan","76":"Brazil","156":"China","250":"France","702":"Singapore","784":"United Arab Emirates","710":"South Africa","682":"Saudi Arabia","724":"Spain","380":"Italy","528":"Netherlands","752":"Sweden","756":"Switzerland","578":"Norway","208":"Denmark","616":"Poland","372":"Ireland","554":"New Zealand"};
const focus:Record<string,[number,number]>={India:[78.9,20.5],"United States":[-100,39],Canada:[-106,57],"United Kingdom":[-3,55],Australia:[134,-25],Germany:[10.4,51.2],France:[2,46],Singapore:[103.8,1.35],Japan:[138,36],China:[104,35],Brazil:[-52,-10],"United Arab Emirates":[54,24],"Saudi Arabia":[45,24],"South Africa":[24,-30],Spain:[-3.5,40],Italy:[12.5,42.8],Netherlands:[5.3,52.1],Sweden:[15,62],Switzerland:[8.2,46.8],Norway:[8,62],Denmark:[10,56],Poland:[19,52],Ireland:[-8,53],"New Zealand":[172,-41]};
export default function Globe({location,onLocation,leads=[]}:Props){const[rotation,setRotation]=useState<[number,number]>([0,0]),[zoom,setZoom]=useState(1),[hover,setHover]=useState(""),[playing,setPlaying]=useState(true),[drag,setDrag]=useState(false);const start=useRef({x:0,y:0,lon:0,lat:0}),frame=useRef<number|null>(null),last=useRef(0);const geo=useMemo(()=>((feature(countries as any,(countries as any).objects.countries) as any).features as Country[]),[]);const width=900,height=620,cx=450,cy=310,base=245;const projection=useMemo(()=>geoOrthographic().translate([cx,cy]).scale(base*zoom).clipAngle(90).rotate([-rotation[0],-rotation[1],0]),[rotation,zoom]);const path=useMemo(()=>geoPath(projection),[projection]);const countryName=(g:Country)=>String(g.properties?.name||g.properties?.NAME||g.properties?.ADMIN||names[String(g.id??"")]||"Region");useEffect(()=>{const f=focus[location];if(f){setRotation(f);setZoom(1.18)}},[location]);useEffect(()=>{if(!playing||drag)return;last.current=performance.now();const tick=(now:number)=>{const dt=now-last.current;if(dt>=24){last.current=now;setRotation(r=>[r[0]+dt*.0032,r[1]])}frame.current=requestAnimationFrame(tick)};frame.current=requestAnimationFrame(tick);return()=>{if(frame.current)cancelAnimationFrame(frame.current)}},[playing,drag]);const down=(e:React.PointerEvent<SVGSVGElement>)=>{setDrag(true);e.currentTarget.setPointerCapture(e.pointerId);start.current={x:e.clientX,y:e.clientY,lon:rotation[0],lat:rotation[1]};setPlaying(false)};const move=(e:React.PointerEvent<SVGSVGElement>)=>{if(!drag)return;const dx=e.clientX-start.current.x,dy=e.clientY-start.current.y;setRotation([start.current.lon-dx*.28,Math.max(-78,Math.min(78,start.current.lat+dy*.2))])};const reset=()=>{setPlaying(false);setRotation([0,0]);setZoom(1)};return <div className="globe-canvas" style={{position:"relative",width:"100%",height:"100%",touchAction:"none"}}><svg viewBox={`0 0 ${width} ${height}`} width="100%" height="100%" onPointerDown={down} onPointerMove={move} onPointerUp={()=>setDrag(false)} onPointerCancel={()=>setDrag(false)} onWheel={e=>{e.preventDefault();setZoom(z=>Math.max(.72,Math.min(2.7,z*(e.deltaY<0?1.08:.92))))}} style={{display:"block",cursor:drag?"grabbing":"grab"}}><defs><radialGradient id="globeGlow"><stop offset="0" stopColor="#ecfff7"/><stop offset=".76" stopColor="#d4f7e8"/><stop offset="1" stopColor="#aee4cf"/></radialGradient><filter id="shadow"><feDropShadow dx="0" dy="14" stdDeviation="18" floodOpacity=".13"/></filter></defs><circle cx={cx} cy={cy} r={base*zoom+15} fill="#55d9a5" opacity=".08" filter="url(#shadow)"/><circle cx={cx} cy={cy} r={base*zoom} fill="url(#globeGlow)" stroke="#69c9a5" strokeWidth="1.2"/>{geo.map(g=>{const d=path(g as any);if(!d)return null;const n=countryName(g),selected=n.toLowerCase()===location.toLowerCase(),f=focus[n];return <path key={String(g.id)} d={d} onPointerEnter={()=>setHover(n)} onPointerLeave={()=>setHover("")} onClick={e=>{e.stopPropagation();setPlaying(false);onLocation(n);if(f){setRotation(f);setZoom(1.22)}}} fill={selected?"#76d8ae":"#f7fffb"} stroke={selected?"#087f68":"#78bfa5"} strokeWidth={selected?1.7:.62} className="country-shape"/>})}{leads.filter(l=>l.lat!=null&&l.lng!=null).map((l,i)=>{const p=projection([Number(l.lng),Number(l.lat)]);if(!p)return null;const visible=Math.hypot(Number(p[0])-cx,Number(p[1])-cy)<=base*zoom+1;return visible?<g key={`${l.name}-${i}`} className="lead-marker"><circle cx={p[0]} cy={p[1]} r={7} fill="#18a878" opacity=".14"/><circle cx={p[0]} cy={p[1]} r={3.6} fill="#087f68" stroke="#fff" strokeWidth="1.8"><title>{l.name} · {l.rating??"—"}★ · {l.review_count} reviews</title></circle></g>:null})}</svg><div className="globe-overlay"><span>{leads.length?"LIVE DISCOVERY GLOBE":"GLOBAL MARKET EXPLORER"}</span><b>{location}</b><small>{hover?`Hovering ${hover}`:"Drag to rotate · scroll to zoom · click a country"}</small></div><div className="map-tools"><button className="world-reset" onClick={reset}>⌖ Center</button><button className="world-reset" onClick={()=>setPlaying(v=>!v)}>{playing?"❚❚ Pause":"▶ Rotate"}</button>{leads.length>0&&<span className="map-result-badge">● {leads.length} discovered</span>}</div><div className="map-legend"><span><i className="legend-dot selected-dot"/> Selected</span><span><i className="legend-dot border-dot"/> Countries</span>{leads.length>0&&<span><i className="legend-dot lead-dot"/> Leads</span>}</div></div>}
